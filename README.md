This project is a simple AI-based system that compares resumes with a given job description and ranks candidates based on how well they match.

The idea is similar to how ATS (Applicant Tracking Systems) work in real hiring processes.

It takes resumes (PDF), extracts text, processes it, and calculates a score using both:

text similarity
skill matching

Then it ranks all resumes from best to worst.
